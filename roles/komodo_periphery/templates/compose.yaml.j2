services:
  periphery:
    container_name: komodo-periphery
    image: {{ komodo_periphery_image }}
    restart: unless-stopped
    user: "${UID}:${GID}"
    group_add:
      - "${DOCKER_GID}"
    labels:
      komodo.skip:
    env_file:
      - .env
    volumes:
      - /var/run/docker.sock:/var/run/docker.sock
      - /proc:/proc
      - {{ komodo_periphery_project_directory }}/shared/keys:/config/keys
      - {{ komodo_periphery_root_directory }}:{{ komodo_periphery_root_directory }}
    networks:
      - internal

networks:
  internal:
    name: komodo-internal
    driver: bridge
